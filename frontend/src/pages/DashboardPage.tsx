export function DashboardPage() {
    return (
        <div className="text-center py-20">
            <h1 className="text-4xl font-bold mb-4 bg-gradient-to-r from-[var(--primary)] to-[var(--accent)] bg-clip-text text-transparent">
                Welcome to SaaS Core
            </h1>
            <p className="text-[var(--muted)] text-lg max-w-2xl mx-auto">
                Select an option from the sidebar to manage your multi-tenant authentication system.
            </p>
        </div>
    );
}
